# WalletConnect/Reown project id — required ONLY for the WalletConnect → Sage path
# (get one free at https://cloud.reown.com). Not needed inside the DIG Browser,
# which injects its own in-process wallet as window.chia.
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=
NEXT_PUBLIC_COINSET_BASE_URL=https://api.coinset.org
